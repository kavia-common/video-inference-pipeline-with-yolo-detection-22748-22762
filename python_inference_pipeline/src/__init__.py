"""
python_inference_pipeline package.

This package contains:
- A FastAPI app (src.api) for health/status and optional async job execution.
- A CLI (src.cli) to run the video inference pipeline as a batch job.
"""
